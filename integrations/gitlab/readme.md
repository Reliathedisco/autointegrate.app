# GitLab Integration

Repos, issues, merge requests, and pipelines.

## Templates

- `init.ts` - GitLab API client
- `issues.ts` - Issue and MR management

## Environment Variables

```env
GITLAB_TOKEN=glpat-xxx
```
