# Redis Integration

In-memory caching and data store.

## Templates

- `init.ts` - Redis client connection
- `client.ts` - Cache and store helpers

## Environment Variables

```env
REDIS_URL=redis://xxx
```
