{
  "name": "stripe",
  "version": "1.0.0",
  "templates": {
    "init": {
      "description": "Initialize Stripe client",
      "output": "lib/stripe.ts"
    },
    "checkout": {
      "description": "Checkout session helpers",
      "output": "lib/stripe-checkout.ts"
    },
    "webhook": {
      "description": "Webhook handler",
      "output": "app/api/webhooks/stripe/route.ts"
    }
  }
}
