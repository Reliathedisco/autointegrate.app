{
  "name": "webhook",
  "version": "1.0.0",
  "templates": {
    "init": {
      "description": "Signature verification utilities",
      "output": "lib/webhook.ts"
    },
    "handler": {
      "description": "Webhook route handler",
      "output": "app/api/webhooks/route.ts"
    },
    "queue": {
      "description": "Async webhook processing",
      "output": "lib/webhook-queue.ts"
    }
  }
}
